environment:
  matrix:
  - TEST: score
  - TEST: sample
  - TEST: validate

init:
- cmd: choco install make
- set PATH=C:\Python37-x64;%PATH%

install:
- pip install .

build: off

test_script:
- make -C data/%TEST%
